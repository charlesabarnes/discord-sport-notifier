<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Discord Sport Notifier</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
  <script src="https://unpkg.com/htmx.org@1.9.10"></script>
  <script src="https://unpkg.com/alpinejs@3.13.3/dist/cdn.min.js" defer></script>
</head>
<body class="bg-gray-100 min-h-screen">
  <div class="container mx-auto px-4 py-8">
    <header class="mb-8 flex flex-wrap items-center justify-between">
      <div>
        <h1 class="text-3xl font-bold text-gray-800">Discord Sport Notifier</h1>
        <p class="text-gray-600">Configure your sports notifications by environment</p>
      </div>
      
      {{#if user}}
      <div class="flex items-center space-x-4">
        <div class="flex items-center">
          <img 
            src="https://cdn.discordapp.com/avatars/{{user.id}}/{{user.avatar}}.png" 
            alt="{{user.username}}" 
            class="w-10 h-10 rounded-full mr-2"
          >
          <span class="text-gray-800 font-medium">{{user.username}}</span>
        </div>
        <a href="/logout" class="text-red-500 hover:text-red-700 text-sm font-medium">Logout</a>
      </div>
      {{/if}}
    </header>

    <div class="bg-white shadow-md rounded-lg p-6">
      {{{body}}}
    </div>

    <footer class="mt-8 text-center text-gray-500 text-sm">
      <p>&copy; 2025 Discord Sport Notifier</p>
    </footer>
  </div>
  <script src="/js/app.js"></script>
</body>
</html>